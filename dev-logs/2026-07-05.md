# 开发日志 - 2026-07-05

## 会话

- 任务：记录网络故障时启用可用 VPN 节点的工作规则。
- 需求来源：用户请求“当遇到网络延迟或者是其他网络问题时启动VPN可用的节点，这个记录到dw里面”。
- 当前阶段：文档规则更新。

## 已完成

- 在 `AGENTS.md` 登记本地 `vpn-mihomo` 技能。
- 在核心流程中新增网络延迟或失败时按需启用可用 VPN 节点的规则。
- 在开发流程中新增“网络故障与 VPN 配方”。
- 在执行清单中加入联网任务、验证失败和订阅检查的代理边界。
- 将规则融合上传到 `q547113058-max/-hermes-vpn-skills`。
- 在 `-hermes-vpn-skills` 新增 `vpn-mihomo-codex/SKILL.md`，对齐 Codex/Mihomo 与 `dw-skills` 网络故障规则。
- 更新 `-hermes-vpn-skills` README，并将 V2Ray 测速脚本的订阅 token 改为环境变量读取。
- 修改本地 `vpn-mihomo` 技能脚本，支持 base64 V2Ray 订阅转换为 Mihomo YAML。
- 新增本地 `vpn-mihomo check` 动作：只检查订阅、转换配置并运行 `mihomo -t`，不启动代理、不改系统代理。
- 修改本地 `vpn-mihomo start`：配置校验通过且端口真实监听后才启用系统代理。
- 更新 `-hermes-vpn-skills` 的 `vpn-mihomo-codex/SKILL.md`，记录 `check` 和安全转换行为。

## 变更文件

- `AGENTS.md`
- `docs/02-development-workflow.md`
- `docs/05-execution-checklist.md`
- `dev-logs/2026-07-05.md`
- GitHub：`q547113058-max/-hermes-vpn-skills`
- `C:\Users\54711\.codex\skills\vpn-mihomo\SKILL.md`
- `C:\Users\54711\.codex\skills\vpn-mihomo\scripts\vpn-mihomo.ps1`

## 决策

- GitHub、包下载、外部 API、浏览器登录或 DNS/超时问题可触发 `vpn-mihomo` 检查和节点测试。
- 命令行联网任务优先使用当前 shell 的 `HTTP_PROXY` / `HTTPS_PROXY`，避免默认改全局系统代理。
- 仅检查订阅或配置有效性时，不启动代理、不改系统代理、不输出订阅链接或节点凭据。
- 远端 VPN 技能仓库只保存无密钥技能说明和脚本；真实订阅 URL、token、节点服务器、UUID、密码和完整配置不得上传。
- 本地 `vpn-mihomo.ps1` 可保留私有订阅 URL，但不得原样上传到公共仓库；公共仓库只记录无密钥流程。

## 验证

- `git diff --check`：通过；仅有 Windows 换行提示。
- `rg` 检查相关文件：确认只记录代理端口、流程和保密规则，没有写入真实订阅 URL、节点服务器、UUID、密码或完整配置。
- 已审查相关 Markdown diff，改动聚焦在网络故障与 VPN 使用边界。
- GitHub API 上传 `-hermes-vpn-skills`：成功更新 README、`vpn-mihomo-codex/SKILL.md`、V2Ray 技能说明和测速脚本。
- 远端 `speedtest.py`：`python -m py_compile` 通过。
- 远端敏感信息扫描：未发现旧硬编码 token、本地 `$SubscriptionUrl` 或 token 字面量。
- 本地 `vpn-mihomo check`：通过，返回 `Fetch=OK`、`Format=base64-v2ray`、`NodeCount=46`、`Protocols=vless=13, vmess=33`、`MihomoConfigTest=PASS`。
- 本地代理状态验证：`ProxyEnable=0`，`17890` / `19090` 未监听；订阅检查未启动代理、未修改系统代理。

## 未解决问题

- 本地 `main` 仍显示相对 `origin/main` 领先，这是此前通过 GitHub API 同步远端后本地 remote-tracking 引用未刷新的遗留状态。

## 下一步待办

- 如需要同步 GitHub，优先解决或绕过本机 Git HTTPS 推送问题。
