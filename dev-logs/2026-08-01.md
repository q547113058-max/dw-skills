# 2026-08-01

## Finesse UI 整仓安装

- 用户明确授权完整安装 `mouse-lin/finesse-skill` 并加入 DW。
- 完整仓库安装到 `work/finesse-skill`，保留上游全部平台副本、示例、资源、参考文档和 Git 元数据。
- 通过 `D:\Codex\home\skills\finesse-ui` 目录联接暴露仓库内唯一主 Skill，不复制第二份运行内容。
- 安装版本为 `finesse-ui 0.12.0`，提交 `ba004b21e14e55385992dff6e345db7108deca78`，许可证 MIT。
- DW 增加真实 UI 任务的条件路由和 30 天使用前更新检查；一般开发任务不加载。
- 明确不启用上游根 `AGENTS.md`、插件、hooks 或平台配置，不自动创建 `PRODUCT.md`、`design-model.yaml`，固定审美处方不升级为 DW 硬门禁。
- 首次直连 Git 克隆遇到 `getaddrinfo() thread failed to start`；使用本地 Mihomo 命令级代理完成，未启用 Windows 系统代理。

## 验证

- 上游仓库 `git fsck --connectivity-only` 通过，工作树干净，主分支 `main`，受上游 Git 管理的文件为 177 个。
- DW `git check-ignore` 确认 `work/finesse-skill` 命中根 `.gitignore`；DW 对该目录的跟踪文件数为 0，只记录安装路径和集成规则。
- `D:\Codex\home\skills\finesse-ui` 为有效目录联接，`SKILL.md` 可读，frontmatter 版本为 `0.12.0`。
- `check-dw-skill-updates.ps1 mark -Skill finesse-ui` 成功，本地和远端提交一致；随后 `check` 返回 `not-due`。
- PowerShell AST 解析和 `git diff --check` 通过；安装仓库的私钥、常见 token 和 API key 形态扫描为 0 个文件。
- `detect.mjs --json` 对自带 dashboard 示例运行成功，`p0=0`；报告包含 7 条 P2 `pure-bw` 提示，因此仍按辅助工具而非完成门禁使用。
- Mihomo 本地端口 `17890`、`19090` 已关闭；脚本停止时曾错误恢复系统代理为启用，已按启动前快照恢复 `ProxyEnable=0`，保留原服务器字符串不变。

## Browser Harness 整仓安装

- 用户明确要求完全使用 `browser-use/browser-harness` 并由 DW 记录安装路径。
- 完整仓库固定安装到 `work/browser-harness`，版本 `v0.1.8`，提交 `dbe6f8f22ba65170e2d4b8f17754c704d008fe49`；完整仓库不加入 DW Git。
- `D:\Codex\home\skills\browser-harness` 指向完整仓库根目录；运行时命令为 `C:\Users\54711\.local\bin\browser-harness.exe`。
- 使用本地 `uv tool install --python 3.12` 完成安装；版本检查返回 `0.1.8`。
- 遥测配置和环境变量均关闭，录屏关闭，域名 Skill 未启用。
- 上游 `browser-harness skill` 在 `v0.1.8` 只输出 `../../SKILL.md` 指针，未覆盖上游文件，直接使用仓库根入口。
- `check-dw-skill-updates.ps1 mark -Skill browser-harness` 成功，随后 `check` 返回本地和远端状态 `up-to-date`。
- `browser-harness telemetry status` 确认 `disabled_by_config=true`；录屏状态为 `off (config)`；`BH_DOMAIN_SKILLS` 未设置。
- 仓库工作树干净，提交 `work/browser-harness/**` 数为 0，目录联接和 `browser-harness --version` 验证通过。
- 临时 Mihomo 已停止；系统代理保持启动前的 `ProxyEnable=1`、`ProxyServer=127.0.0.1:3067`，Mihomo 端口无监听。
