# 2026-07-28

## 可复用执行规则增量

- 对未明确验收的 `standard`、`high-risk` 任务，采用最低通过条件、质量目标和失败边界；需求清楚时不重复确认。
- 复杂任务只保留一个计划和少量阶段检查点，`quick` 不创建形式化计划。
- 自动化遵循手工跑通、SOP、限域半自动、全自动的成熟顺序，权限从只读/dry-run 逐步扩大。
- 多 Agent 并发写入使用 worktree 或独立副本；新线程仅在用户明确选择或上下文确有必要时启用。
